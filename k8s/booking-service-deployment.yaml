apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: intercity237
  labels:
    app: booking-service
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
        version: v1
    spec:
      containers:
      - name: booking-service
        image: ghcr.io/intercity237/booking-service:latest
        ports:
        - containerPort: 80
        envFrom:
        - secretRef:
            name: intercity237-db-secret
        env:
        - name: RABBITMQ_HOST
          value: rabbitmq
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 20
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: intercity237
spec:
  selector:
    app: booking-service
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
