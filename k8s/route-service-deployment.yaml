apiVersion: apps/v1
kind: Deployment
metadata:
  name: route-service
  namespace: intercity237
  labels:
    app: route-service
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: route-service
  template:
    metadata:
      labels:
        app: route-service
        version: v1
    spec:
      containers:
      - name: route-service
        image: ghcr.io/intercity237/route-service:latest
        ports:
        - containerPort: 80
        envFrom:
        - secretRef:
            name: intercity237-db-secret
        env:
        - name: RABBITMQ_HOST
          value: rabbitmq
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /api.php
            port: 80
          initialDelaySeconds: 20
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /api.php
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: route-service
  namespace: intercity237
spec:
  selector:
    app: route-service
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
