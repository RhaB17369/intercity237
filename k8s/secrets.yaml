# NOTE: In production, use sealed-secrets or Vault.
# Values below are base64-encoded. Generate with: echo -n "value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: intercity237-db-secret
  namespace: intercity237
type: Opaque
stringData:
  DB_HOST: db
  DB_NAME: intercity237
  DB_USER: intercity237
  DB_PASS: Intercity237!
  APP_URL: http://localhost
  BASE_URL: ""
  SITE_NAME: Intercity237
  MAIL_FROM: noreply@intercity237.cm
