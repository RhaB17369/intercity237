apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
  namespace: intercity237
  labels:
    app: ticket-service
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: ticket-service
  template:
    metadata:
      labels:
        app: ticket-service
        version: v1
    spec:
      containers:
      - name: ticket-service
        image: ghcr.io/intercity237/ticket-service:latest
        ports:
        - containerPort: 80
        envFrom:
        - secretRef:
            name: intercity237-db-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "150m"
        livenessProbe:
          httpGet:
            path: /scan.php
            port: 80
          initialDelaySeconds: 20
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /scan.php
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: intercity237
spec:
  selector:
    app: ticket-service
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
